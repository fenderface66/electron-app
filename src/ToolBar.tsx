import * as React from 'react';
import { SFC } from 'react';

type Props = {};

const ToolBar: SFC<Props> = () => {
  console.log(history);
  return (
      <p>Hello</p>
  );
};

export default ToolBar;
